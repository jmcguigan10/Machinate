[package]
name = "machinator-ir"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Typed model IR foundation for Machinator"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
